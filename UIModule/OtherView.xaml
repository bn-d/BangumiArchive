<Page
    x:Class="BangumiArchive.UIModule.OtherView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    NavigationCacheMode="Enabled">

    <Grid>
        <ScrollViewer>
            <ListView x:Name="OtherListView"
                      ItemsSource="{x:Bind OtherLists}" ItemTemplate="{StaticResource OtherListItem}" 
                      VerticalContentAlignment="Top" HorizontalAlignment="Center"
                      CanDragItems="True" CanReorderItems="True" AllowDrop="True" 
                      SelectionMode="None" Padding="50"/>
        </ScrollViewer>
        <CommandBar Background="{x:Null}" BorderThickness="0" Margin="0,0,20,0">
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Export" Click="ExportOtherList"/>
                <AppBarButton Label="Import" Click="ImportOtherList"/>
            </CommandBar.SecondaryCommands>

            <AppBarButton Icon="Add" Label="" Click="AddList"/>
            <AppBarButton Icon="Remove" Label="" Click="RemoveList"/>
            <AppBarButton Icon="Save" Label="" Click="SaveData"/>
        </CommandBar>
    </Grid>
</Page>
